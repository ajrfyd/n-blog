import styled from "styled-components";

const Main = () => {

  return (
    <Container>
      Hello Main??
    </Container>
  )
}

export default Main;

const Container = styled.div`
  
`

const Block = styled.div`
  top: 4rem;
`